# Улучшения v3.0 - Общие улучшения скрипта

## 🎯 Что нового

### ✨ 1. Сохранение по имени товара

**Было:**

```
scripts/downloaded_images/product-1731385920123.jpg
scripts/downloaded_images/product-1731385925456.png
```

**Стало:**

```
scripts/downloaded_images/IP_camera_HIWATCH_DS-I650M.jpg
scripts/downloaded_images/HDVR_HIWATCH_DS-H116GA.png
scripts/downloaded_images/Mouse_Logitech_G502.jpg
```

#### Особенности:

- ✅ Использует название товара из Saleor
- ✅ Очищает недопустимые символы (<>:"/\\|?\*)
- ✅ Заменяет пробелы на underscore
- ✅ Ограничивает длину до 100 символов
- ✅ Автоматически добавляет счетчик если файл уже существует:
  ```
  IP_camera_HIWATCH_DS-I650M.jpg
  IP_camera_HIWATCH_DS-I650M_1.jpg  ← дубликат
  IP_camera_HIWATCH_DS-I650M_2.jpg  ← еще дубликат
  ```

### ✨ 2. Retry логика

Скрипт теперь делает несколько попыток при ошибках:

```javascript
// По умолчанию: 2 повторные попытки (всего 3 попытки)
MAX_RETRIES=2

// Настроить:
MAX_RETRIES=3 pnpm images:add  // 3 повторные попытки
```

**Как работает:**

```
[1/10] Обработка: IP camera...
  ⚠️  Ошибка: Не удалось найти изображение
  🔄 Повторю попытку через 5 секунд...

[1/10] 🔄 Повторная попытка 1/2: IP camera...
  ✅ Успешно добавлено изображение!
```

**Когда полезно:**

- Временные проблемы сети
- Капча появилась но была решена
- Google временно не ответил

### ✨ 3. Детальная статистика

**Новая статистика:**

```
======================================================================

📊 ИТОГОВАЯ СТАТИСТИКА
======================================================================

✅ Результаты обработки:
   • Успешно обработано:          45 товаров
   • Ошибок (после всех попыток): 3 товаров
   • Пропущено:                   2 товаров
   • Всего обработано:            50 товаров
   • Всего найдено без изображ.:  2296 товаров

⏱️  Время выполнения:
   • Общее время:                 12м 34с
   • Среднее время на товар:      16с

🤖 Статистика капчи:
   • Капч обнаружено:             1
   • Частота капчи:               ~2% от товаров

📈 Успешность:
   • Процент успешных:            90%

======================================================================
```

### ✨ 4. Настраиваемые паузы

**Переменные окружения:**

```bash
# Изменить частоту пауз
PAUSE_EVERY_10=5 pnpm images:add    # Пауза каждые 5 товаров (вместо 10)
PAUSE_EVERY_30=20 pnpm images:add   # Пауза каждые 20 товаров
PAUSE_EVERY_50=40 pnpm images:add   # Пауза каждые 40 товаров

# Изменить количество попыток
MAX_RETRIES=3 pnpm images:add       # 3 повторные попытки

# Комбинировать:
MAX_RETRIES=3 PAUSE_EVERY_10=5 IMAGES_LIMIT=50 pnpm images:add
```

**Для агрессивного режима (быстро, больше капч):**

```bash
PAUSE_EVERY_10=20 PAUSE_EVERY_30=50 PAUSE_EVERY_50=100 pnpm images:add
```

**Для осторожного режима (медленно, меньше капч):**

```bash
PAUSE_EVERY_10=5 PAUSE_EVERY_30=15 PAUSE_EVERY_50=25 pnpm images:add
```

### ✨ 5. Улучшенное логирование

**Каждый товар:**

```
[1/50] 📦 Обработка: IP camera HIWATCH DS-I650M
    🔍 Открываю Google: "IP camera HIWATCH DS-I650M..."
    ⌨️  Ввожу запрос...
    🖱️  Перехожу на вкладку Картинки...
    📸 Ищу изображения...
    🖱️  Кликаю на первое изображение...
    ✓ Кликнул на изображение
    🔍 Извлекаю URL полноразмерного изображения...
    ✓ Найдено изображение:
      URL: https://example.com/image.jpg
      Длина URL: 156 символов
      Валидный URL: Да
    🌐 Скачиваю изображение...
      URL: https://example.com/image.jpg...
      Content-Type: image/jpeg
      Размер: 156.23 KB
      Расширение: .jpg
      ✓ Сохранено во временную папку: IP_camera_HIWATCH_DS-I650M.jpg
  ⬆️  Загрузка изображения в Saleor...
    📎 Тип файла: image/jpeg
    📎 Имя файла: product.jpg
    ✅ Изображение загружено в Saleor:
       ID: ProductMedia:123
       URL: https://saleor.com/media/products/abc.jpg
       Тип: IMAGE
    🔗 Изображение будет доступно по ссылке
    💾 Изображение сохранено: IP_camera_HIWATCH_DS-I650M.jpg
  🏷️  Добавление метаданных (autoImage: true)...
  ✅ Успешно добавлено изображение! (16с)
```

### ✨ 6. Счетчик времени

Каждый товар показывает сколько времени занял:

```
✅ Успешно добавлено изображение! (16с)
✅ Успешно добавлено изображение! (1м 2с)
```

### ✨ 7. Счетчик капч

Теперь видите сколько капч появилось:

```
⚠️  🤖 ОБНАРУЖЕНА КАПЧА!
⏳ Ожидаю решения капчи...
[решаете капчу]
✅ Капча решена! Продолжаю работу...

...

⚠️  Капча обнаружена (всего капч: 2)
```

## 📋 Сравнение версий

| Функция                  | v1.0 | v2.0 (капча) | v3.0 (улучшения) |
| ------------------------ | ---- | ------------ | ---------------- |
| Поиск изображений        | ✅   | ✅           | ✅               |
| Скачивание               | ✅   | ✅           | ✅               |
| Загрузка в Saleor        | ✅   | ✅           | ✅               |
| Предотвращение засыпания | ✅   | ✅           | ✅               |
| Определение капчи        | ❌   | ✅           | ✅               |
| Ожидание капчи           | ❌   | ✅           | ✅               |
| Случайные задержки       | ❌   | ✅           | ✅               |
| Умные паузы              | ❌   | ✅           | ✅               |
| **Сохранение по имени**  | ❌   | ❌           | ✅               |
| **Retry логика**         | ❌   | ❌           | ✅               |
| **Детальная статистика** | ❌   | ❌           | ✅               |
| **Настраиваемые паузы**  | ❌   | ❌           | ✅               |
| **Счетчик времени**      | ❌   | ❌           | ✅               |
| **Счетчик капч**         | ❌   | ❌           | ✅               |

## 🚀 Примеры использования

### Пример 1: Обычный режим

```bash
IMAGES_LIMIT=50 pnpm images:add
```

Результат:

- Сохранит 50 изображений по именам товаров
- Сделает 2 попытки при ошибках
- Паузы каждые 10/30/50 товаров
- Покажет детальную статистику

### Пример 2: С сохранением файлов

```bash
KEEP_IMAGES=true IMAGES_LIMIT=20 pnpm images:add
```

Результат:

- Сохранит файлы в `scripts/downloaded_images/`
- С именами товаров!
- Можно открыть: `open scripts/downloaded_images/`

### Пример 3: Агрессивный режим

```bash
MAX_RETRIES=3 PAUSE_EVERY_10=20 IMAGES_LIMIT=100 pnpm images:add
```

Результат:

- Быстрее обработка (меньше пауз)
- Больше попыток (3 вместо 2)
- Ожидайте больше капч

### Пример 4: Осторожный режим

```bash
PAUSE_EVERY_10=5 PAUSE_EVERY_30=15 IMAGES_LIMIT=50 pnpm images:add
```

Результат:

- Медленнее обработка (больше пауз)
- Меньше капч
- Более безопасно

## 🔧 Технические детали

### Функция очистки имени

```javascript
function sanitizeFilename(name) {
  return name
    .replace(/[<>:"/\\|?*]/g, "") // Удаляем недопустимые символы
    .replace(/\s+/g, "_") // Пробелы → underscore
    .replace(/_{2,}/g, "_") // __ → _
    .replace(/^[._]+|[._]+$/g, "") // Удаляем в начале/конце
    .substring(0, 100); // Макс 100 символов
}
```

Примеры:

```javascript
sanitizeFilename("IP camera HIWATCH DS-I650M (2.8 mm)");
// → "IP_camera_HIWATCH_DS-I650M_2.8_mm"

sanitizeFilename('Mouse "Pro Gaming" <New>');
// → "Mouse_Pro_Gaming_New"

sanitizeFilename("A".repeat(150));
// → "A".repeat(100)  // Обрезано до 100
```

### Дедупликация файлов

```javascript
// Если файл существует, добавляем счетчик
let finalPath = savedPath;
let counter = 1;
while (fs.existsSync(finalPath)) {
  const ext = path.extname(filename);
  const nameWithoutExt = path.basename(filename, ext);
  finalPath = path.join(downloadedDir, `${nameWithoutExt}_${counter}${ext}`);
  counter++;
}
```

### Форматирование времени

```javascript
function formatDuration(ms) {
  const seconds = Math.floor(ms / 1000);
  const minutes = Math.floor(seconds / 60);
  const remainingSeconds = seconds % 60;

  if (minutes > 0) {
    return `${minutes}м ${remainingSeconds}с`;
  }
  return `${seconds}с`;
}
```

## 🎉 Заключение

Скрипт теперь:

- ✅ Сохраняет файлы с читаемыми именами
- ✅ Делает повторные попытки при ошибках
- ✅ Показывает детальную статистику
- ✅ Настраивается через переменные окружения
- ✅ Логирует каждый шаг подробно
- ✅ Считает время и капчи

**Версия:** 3.0.0
**Дата:** 2024-11-12
