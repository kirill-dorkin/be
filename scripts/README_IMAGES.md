# Автоматическое добавление изображений к продуктам

## Как это работает

Скрипт подключается к вашему уже открытому Chrome браузеру через Chrome DevTools Protocol. Это позволяет:

- ✅ Использовать ваш настоящий профиль Chrome с авторизацией в Google
- ✅ Избежать капчи от Google
- ✅ Видеть процесс поиска изображений в реальном времени
- ✅ Не создавать новый браузер с временным профилем

## Будут ли изображения доступны?

**ДА!** Изображения загружаются как **файлы** в Saleor:

1. 🔍 Скрипт находит изображение в Google Images
2. 🖱️ Кликает на первое изображение и получает **полноразмерную версию**
3. 💾 **Скачивает файл** на ваш компьютер (во временную папку)
4. ⬆️ **Загружает файл в Saleor** через GraphQL мутацию `productMediaCreate`
5. ✅ Saleor сохраняет файл в своем хранилище (S3, GCS или локально)
6. 🔗 Saleor возвращает **постоянный URL** изображения

**Изображения не хранятся по внешним ссылкам!** Они полностью загружаются в ваш Saleor и будут доступны по URL вашего домена.

**Проверка:** После добавления изображений запустите `pnpm images:check` чтобы проверить доступность всех загруженных изображений.

## Инструкция по использованию

### Шаг 1: Запустите Chrome с remote debugging

**Рекомендуется (автоматический запуск):**
```bash
pnpm chrome:debug
```

Эта команда:
- Закроет все открытые процессы Chrome
- Запустит Chrome с remote debugging на порту 9222
- Создаст постоянный профиль в `~/.chrome-debug-profile`
- Проверит что порт доступен

**Вариант Б (вручную):**
```bash
# 1. Закройте все процессы Chrome
pkill -9 -i chrome

# 2. Создайте директорию для профиля
mkdir -p ~/.chrome-debug-profile

# 3. Запустите Chrome
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
  --remote-debugging-port=9222 \
  --user-data-dir="$HOME/.chrome-debug-profile" &
```

**Важно:**
- Chrome должен быть запущен с флагом `--remote-debugging-port=9222`
- Обязательно нужен флаг `--user-data-dir` (Chrome требует отдельный профиль для remote debugging)
- Профиль будет сохранен и использоваться при последующих запусках

### Шаг 2: Войдите в Google (если нужно)

1. Откройте любую вкладку в Chrome
2. Перейдите на google.com
3. Войдите в свой Google аккаунт (если еще не вошли)

Это нужно сделать один раз. После этого Google будет помнить вас и не будет показывать капчу.

### Шаг 3: Запустите скрипт добавления изображений

**Обработать все продукты:**
```bash
pnpm images:add
```

**Обработать только первые 3 продукта (для теста):**
```bash
IMAGES_LIMIT=3 pnpm images:add
```

**Обработать только 1 продукт:**
```bash
IMAGES_LIMIT=1 pnpm images:add
```

**Сохранить скачанные изображения (не удалять):**
```bash
KEEP_IMAGES=true IMAGES_LIMIT=3 pnpm images:add
```

Изображения будут сохранены в папке `scripts/downloaded_images/`

**⚡ ВАЖНО:** Скрипт автоматически предотвращает засыпание экрана!

Используется утилита `caffeinate -d` (встроенная в macOS):
- ✅ Экран не будет тухнуть во время работы
- ✅ Mac не уйдет в сон
- ✅ Chrome останется активным
- ✅ После завершения скрипта всё вернется в норму

**🤖 ЗАЩИТА ОТ КАПЧИ:**

Скрипт использует умные техники:
- ✅ **Автоматическое определение капчи** - скрипт детектирует когда появилась капча
- ✅ **Случайные задержки** - имитация человеческого поведения
- ✅ **Умные паузы** - автоматические паузы после каждых 10/30/50 товаров
- ✅ **Проверка успешности** - убедится что изображение найдено

**🚀 АВТОМАТИЧЕСКОЕ РЕШЕНИЕ КАПЧИ (НОВИНКА!):**

Интегрируйте **anti-captcha.com** для **полной автоматизации**:
- ✅ **Капча решается автоматически** без вашего участия
- ✅ **Обрабатывайте тысячи товаров** без присмотра
- ✅ **Запускайте на ночь** - утром всё готово
- ✅ **Стоимость:** ~$0.01 за 100 товаров (1000 капч = $1)

**Быстрая настройка:**
1. Зарегистрируйтесь на https://anti-captcha.com
2. Пополните баланс ($5-10 хватит надолго)
3. Получите API ключ: https://anti-captcha.com/clients/settings/apisetup
4. Добавьте в `.env`: `ANTICAPTCHA_API_KEY=ваш_ключ`
5. Готово! Теперь капча решается автоматически

**Подробная инструкция:** `cat scripts/ANTICAPTCHA_SETUP.md`

**Ручной режим:**
Если anti-captcha не настроен, скрипт ждет пока вы решите капчу вручную и продолжает работу.

**Рекомендация:**
- С anti-captcha: обрабатывайте любое количество товаров
- Без anti-captcha: обрабатывайте по 50-100 товаров за раз

Подробнее: `cat scripts/CAPTCHA_PREVENTION.md`

### Шаг 4: Наблюдайте за процессом

Скрипт будет:
1. Подключаться к вашему открытому Chrome
2. Создавать новые вкладки для поиска изображений
3. Искать изображения в Google Images
4. Скачивать и загружать их в Saleor

Вы увидите весь процесс в браузере в реальном времени.

## Где посмотреть скачанные изображения?

### 1. **Проверить загруженные в Saleor** ⭐

После добавления изображений можно проверить их доступность:

```bash
pnpm images:check
```

Эта команда:
- Находит продукты с метаданными `autoImage=true`
- Показывает URL изображений
- **Проверяет доступность каждого изображения** (HTTP запрос)
- Показывает размер и тип файла

**Пример вывода:**
```
✅ Найдено 5 продуктов с автоматическими изображениями

[1/5] IP camera HIWATCH DS-I650M(C)
   📸 Изображений: 1

   Изображение 1:
      URL: https://your-saleor.com/media/product.jpg
      🔍 Проверяю доступность...
      ✅ Изображение ДОСТУПНО!
         Status: 200
         Content-Type: image/jpeg
         Размер: 156.23 KB
```

### 2. **Посмотреть сохраненные файлы на диске**

По умолчанию скрипт **удаляет** временные файлы после загрузки в Saleor.

Чтобы **сохранить** скачанные изображения, используйте флаг `KEEP_IMAGES=true`:

```bash
KEEP_IMAGES=true IMAGES_LIMIT=3 pnpm images:add
```

Файлы будут сохранены в:
```
scripts/downloaded_images/product-1234567890.jpg
scripts/downloaded_images/product-1234567891.png
scripts/downloaded_images/product-1234567892.webp
```

Открыть папку:
```bash
open scripts/downloaded_images/
```

### 3. **Скриншоты отладки**

При ошибках скрипт сохраняет скриншоты в `/tmp/`:
```bash
ls -lht /tmp/debug-*.png
```

## Удаление автоматически добавленных изображений

Если нужно удалить все автоматически добавленные изображения:

```bash
pnpm images:remove-auto
```

## Устранение проблем

### Ошибка "Не удалось подключиться к Chrome"

**Причина:** Chrome не запущен с флагом remote debugging

**Решение:**
1. Закройте все окна Chrome
2. Запустите Chrome с флагом: `pnpm chrome:debug`
3. Повторите попытку запуска скрипта

### Chrome уже запущен

Если Chrome уже запущен (без флага remote debugging):

1. Закройте все окна Chrome полностью (Cmd+Q)
2. Запустите Chrome с флагом: `pnpm chrome:debug`
3. Запустите скрипт: `pnpm images:add`

### Google показывает капчу

Если Google все равно показывает капчу:

1. Пройдите капчу вручную в браузере
2. Google запомнит вас и больше не будет показывать капчу
3. Продолжите работу скрипта

### Ошибка "Connection closed" когда тухнет экран

**Решение:** Уже исправлено! ✅

Скрипт теперь автоматически использует `caffeinate -d` который:
- Предотвращает засыпание экрана
- Держит Mac активным во время работы
- Автоматически отключается после завершения

**Проверьте что используете последнюю версию:**
```bash
pnpm images:add  # должна быть команда с caffeinate
```

Если всё равно возникают проблемы, можно запустить вручную:
```bash
caffeinate -d pnpm images:add
```

## Технические детали

- **Браузер:** Google Chrome с вашим обычным профилем
- **Протокол:** Chrome DevTools Protocol (CDP)
- **Порт:** 9222 (localhost)
- **Библиотека:** Puppeteer в режиме подключения (connect)

## Альтернативный подход (старый способ)

Если нужно вернуться к старому способу (создание нового браузера), можно использовать git для восстановления предыдущей версии скрипта.
