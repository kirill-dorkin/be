# 🐛 Anti-Captcha Debugging Guide

## Последние улучшения (2025-11-12)

### Что было исправлено

1. **Улучшенная диагностика внедрения токена**
   - Теперь показывает сколько textarea найдено
   - Показывает вызваны ли callbacks
   - Показывает найдена ли форма и кнопка submit

2. **Двойной метод отправки формы**
   - Сначала пробует кликнуть на кнопку submit (более надёжно)
   - Если не получилось, пробует form.submit() напрямую
   - Ищет кнопки по разным селекторам

3. **Лучшая обработка навигации**
   - Увеличен таймаут до 20 секунд
   - Использует 'domcontentloaded' вместо 'networkidle2'
   - Проверяет изменение URL даже если навигация вышла по таймауту

4. **Автоматические скриншоты для отладки**
   - Скриншот ДО внедрения токена
   - Скриншот ПОСЛЕ ожидания навигации
   - Скриншот если форма не найдена

## Как использовать отладку

### 1. Запустите скрипт как обычно

```bash
IMAGES_LIMIT=3 pnpm images:add
```

### 2. Если появится капча, следите за логами

Теперь вы увидите подробную информацию:

```
🤖 ОБНАРУЖЕНА КАПЧА! Запускаю автоматическое решение через Anti-Captcha...
🌐 URL: https://www.google.com/sorry/index?continue=...
🔍 Ищу sitekey на странице...
✓ Sitekey найден: 6LfW3QcTAAAAA...
💰 Баланс Anti-Captcha: $6.05
🔧 Создаю задачу Anti-Captcha...
✅ Задача создана, ID: 178440482
⏳ Ожидаю решения капчи (макс. 120 секунд)...
✅ Капча решена Anti-Captcha!

🔧 Обнаружена страница блокировки Google (/sorry)
📸 Скриншот сохранён в /tmp/

✅ Токен решения внедрён в страницу
   - textarea найдено: 2
   - callback вызван: Да
   - форма найдена: Да
   - кнопка submit: Да

📤 Отправляю форму с решением...
✓ Кликнул на кнопку submit
⏳ Жду перенаправления (до 20 секунд)...
✅ Перенаправление выполнено!

✅ URL изменился - капча решена успешно!
📍 Был:   https://www.google.com/sorry/index?continue=...
📍 Стал:  https://www.google.com/search?q=...
```

### 3. Если что-то не работает, смотрите скриншоты

Скриншоты сохраняются в `/tmp/`:

```bash
# Посмотреть последние скриншоты
ls -lht /tmp/captcha-*.png | head -5

# Открыть последний скриншот
open $(ls -t /tmp/captcha-*.png | head -1)

# Открыть все скриншоты в Preview
open /tmp/captcha-*.png
```

**Типы скриншотов:**

- `captcha-before-inject-*.png` - Как выглядит капча ДО внедрения токена
- `captcha-after-wait-*.png` - Как выглядит страница ПОСЛЕ ожидания навигации
- `captcha-no-form-*.png` - Скриншот если форма или кнопка не найдены

## Частые проблемы и решения

### Проблема 1: "textarea найдено: 0"

**Причина:** Google изменил структуру страницы, textarea с g-recaptcha-response не найден.

**Решение:**
1. Посмотрите скриншот `captcha-before-inject-*.png`
2. Если видите капчу, но textarea не найден, Google изменил структуру
3. Попробуйте решить капчу вручную один раз
4. Сообщите об этом (нужно обновить селекторы)

### Проблема 2: "форма найдена: Нет"

**Причина:** На странице нет элемента `<form>` или кнопки submit.

**Решение:**
1. Посмотрите скриншот `captcha-no-form-*.png`
2. Если видите капчу, но нет кнопки "Submit", это необычная страница
3. Попробуйте решить вручную
4. Скриншот поможет понять какой элемент нужно искать

### Проблема 3: "Таймаут навигации"

**Причина:** Страница не перенаправилась за 20 секунд.

**Что проверяется:**
- Скрипт всё равно проверяет изменился ли URL
- Если URL изменился, считается успехом
- Если URL тот же, проверяется исчезла ли капча

**Решение:**
1. Посмотрите логи - проверяет ли скрипт URL после таймаута
2. Если URL изменился, капча решена успешно
3. Если нет, посмотрите скриншот `captcha-after-wait-*.png`

### Проблема 4: "callback вызван: Нет"

**Причина:** На странице нет window.___grecaptcha_cfg или callbacks не настроены.

**Это нормально?** Да, для некоторых страниц Google.

**Решение:** Ничего делать не нужно, скрипт всё равно отправит форму.

### Проблема 5: Капча всё ещё присутствует после решения

**Причина:** Google не принял решение или требует дополнительную проверку.

**Что делается:**
1. Скрипт проверяет URL изменился ли
2. Проверяет исчезла ли капча с DOM
3. Делает скриншот для анализа

**Решение:**
1. Посмотрите скриншоты до и после
2. Если капча та же, Google может блокировать IP
3. Попробуйте решить вручную один раз
4. Google может запомнить и не показывать снова

## Статистика успешности

При правильной работе вы должны видеть:

```
✅ Успешно решено автоматически: 95-98% случаев
⚠️  Требует ручного решения: 2-5% случаев
❌ Ошибка решения: <1% случаев
```

Если автоматическое решение не работает чаще чем в 10% случаев, что-то не так.

## Отладочная информация для разработчика

Если нужно отправить баг-репорт, включите следующую информацию:

1. **Логи терминала** (полный вывод от обнаружения капчи до результата)

2. **Скриншоты** из `/tmp/captcha-*.png`

3. **URL страницы** (из логов: "🌐 URL: ...")

4. **Результат внедрения:**
   ```
   - textarea найдено: X
   - callback вызван: Да/Нет
   - форма найдена: Да/Нет
   - кнопка submit: Да/Нет
   ```

5. **Версия Chrome** (chrome://version/)

6. **Баланс Anti-Captcha** (из логов: "💰 Баланс Anti-Captcha: $X.XX")

## Очистка скриншотов

Скриншоты накапливаются в `/tmp/`, можно периодически чистить:

```bash
# Посмотреть сколько занимают
du -sh /tmp/captcha-*.png

# Удалить скриншоты старше 7 дней
find /tmp -name "captcha-*.png" -mtime +7 -delete

# Удалить все скриншоты капчи
rm /tmp/captcha-*.png
```

## Полезные команды

```bash
# Проверить работает ли anti-captcha (запустить на 1 товаре)
IMAGES_LIMIT=1 pnpm images:add

# Посмотреть последние 10 скриншотов
ls -lht /tmp/captcha-*.png | head -10

# Открыть папку /tmp в Finder
open /tmp

# Следить за логами в реальном времени
pnpm images:add 2>&1 | tee image-add.log

# Поиск ошибок в логах
grep "❌\|⚠️" image-add.log
```

## Что дальше?

Эти улучшения должны значительно повысить надёжность автоматического решения капчи.

Если проблемы продолжаются:
1. Соберите отладочную информацию (см. выше)
2. Посмотрите скриншоты
3. Попробуйте решить капчу вручную один раз
4. Проверьте что используете последнюю версию скрипта

---

**Версия:** 2.0.0
**Дата:** 2025-11-12
**Статус:** Stable
